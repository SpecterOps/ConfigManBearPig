---
id: ope-7313
status: closed
deps: []
links: []
created: 2026-07-22T16:24:52Z
type: task
priority: 2
assignee: Mayyhem
tags: [sccm, cli, help, testing]
---

# Testing panel: reserve rich_help_panel; define its flags (dry-run / auth pre-flight) later

During the collect-sccm --help panel reorg (2026-07-22), the operator asked for a 'Testing' rich_help_panel. It was NOT added because no collect-sccm flag maps to testing today. This ticket reserves the panel and captures the decision to define its flags later. Candidate flags to scope when the need is concrete: --dry-run (run discovery only; list the targets + phases that WOULD run; emit no rows/graph) and/or --check / --test-auth (bind to DC/LDAP and validate creds + reachability, then exit without collecting). When built, add them to collect_sccm() with rich_help_panel='Testing' so they render as their own titled --help section, matching the existing Authentication/Collection/Performance/Output/Logging grouping.

## Notes

**2026-07-31T15:35:17Z**

Status audit 2026-07-31: the reservation this ticket asked for EXISTS -> closing. rich_help_panel='Testing' is now a real titled section on collect_sccm carrying two flags: --run-integration-tests (main.py:1158) and --compare-to-zip (main.py:1164), alongside the Authentication/Collection/Performance/Output/Logging panels. The ticket's deliverable was to reserve the panel and record the decision to define its flags later; the panel is created and populated, so that is satisfied. The two CANDIDATE flags the ticket floated -- --dry-run (discovery only, list targets/phases, emit nothing) and --check/--test-auth (bind and validate credentials, then exit) -- are still unimplemented. Neither was ever committed to; if they are still wanted, open a fresh ticket that scopes them as features rather than keeping this reservation ticket open.
