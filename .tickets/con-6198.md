---
id: con-6198
status: closed
deps: []
links: [con-7741]
created: 2026-07-31T18:32:21Z
type: bug
priority: 2
tags: [sccm, lowpriv, edges, relay]
---

# SCCM_CoerceAndRelayToAdminService not produced by low-priv collection

@_Mayyhem (fixture review 2026-07-31) on edge-coerceandrelaytoadminservice-ps1: 'This edge should be created after unprivileged collection. Investigate.' Currently PASSES privileged, FAILS unprivileged (not found). Per the README's privilege tiers the SCCM_CoerceAndRelayToAdminService family is listed as reachable at the Anonymous/HTTP+DNS tier off role tags and the site hierarchy alone, so a lowpriv run that has the PS1 site and its role tags should produce it. Determine which input is missing at low privilege.

## Acceptance Criteria

A lowpriv collection emits SCCM_CoerceAndRelayToAdminService for the PS1 primary site server; edge-coerceandrelaytoadminservice-ps1 passes unprivileged, or the README's tier table is corrected if the edge genuinely needs privilege.

## Notes

**2026-07-31T21:43:46Z**

NOT REPRODUCIBLE -- closing without a code change. Verified 2026-07-31: edge-coerceandrelaytoadminservice-ps1 reports PASS in BOTH out/unpriv5 and out/unpriv6, and sccm.graph_edges holds exactly 1 SCCM_CoerceAndRelayToAdminService row (start MAYYHEM.COM-S-1-5-11, end PS1) in every dataset on disk -- priv3, priv5, priv6, unpriv3, unpriv4, unpriv5, unpriv6. A parallel investigation plus an independent adversarial verifier both re-queried all seven and confirmed it.
The original failure was LAB STATE, not a collector gap: it was observed in the unpriv3/unpriv4 window when ps1-psv was powered off, which cost that host its SMS Provider tag and truncated the edge's coercionVictimAndRelayTargetPairs list. The README's Anonymous/HTTP+DNS tier claim is accurate -- the SMS Provider tag genuinely comes from the unauthenticated HTTPS 401 probe (collectors/http.py) plus _backfill_bare_site_roles in transforms.py turning a bare 'SMS Provider' into 'SMS Provider@PS1'. No README change needed.
HARDENING SPUN OUT, not required for this ticket (see con-4d81): probe.sms_provider() is HTTPS-only but sits behind  checks driven by port-80 MP/DP probes, so a filtered port 80 silently costs an HTTPS-reachable provider its tag.

**2026-07-31T21:44:56Z**

Correction to the previous note: (a) the hardening ticket is con-7741, not con-4d81 (I wrote the id before filing it); (b) a fragment was eaten by shell backtick expansion -- the sentence should read: probe.sms_provider() is HTTPS-only but sits behind the per-protocol connection_failed short-circuit driven by the port-80 MP/DP probes, so a filtered port 80 silently costs an HTTPS-reachable provider its tag.
