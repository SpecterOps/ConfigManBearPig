---
id: ope-4483
status: closed
deps: []
links: []
created: 2026-06-15T21:43:34Z
type: task
priority: 2
assignee: cthompson
tags: [sccm, smb, collector, collect]
---

# SMB Collection (Port from PowerShell)

Port Invoke-SMBCollection (ConfigManBearPig.ps1:9000) incl. the SMB signing scan (Get-SMBSigningRequiredViaSMBNegotiate, 5113) to a new collectors/smb.py + clients/smb.py. Collect phase only: emit smb_computers (signing via SMB2 negotiate + share-derived roles/flags) and smb_sites rows; classify SCCM shares inline; defer per-host reconciliation + the no-signing relay analysis (6734) to preproc and node/edge build to convert. Reuse connect_smb from smb_sso.py; add negotiated_signing_required + check_smb_signing + list_shares to clients/smb.py. Wire SMB phase after HTTP in per_host_phases.py (skip when AdminService/WMI collected). Add negotiate fallback for smb_signing_required in registry.py (registry DWORD wins). Design doc: docs/superpowers/specs/2026-06-15-smb-collector-design.md

## Notes

**2026-06-15T21:56:31Z**

Collect phase complete. New: clients/smb.py (negotiated_signing_required, check_smb_signing, list_shares), collectors/smb.py (_classify_shares + collect_smb), tests/test_smb.py. Wired SMB phase after HTTP in per_host_phases.py + extended should_run_phase skip. registry.py: negotiate fallback for smb_signing_required (registry DWORD wins) + smb_signing_source field; updated FakeProbe. README updated (collection methods, privileges, codebase tree). Validation: pytest 295 passed / 5 skipped, ruff clean, mypy = baseline noise only (logger.verbose, impacket stubs). NOT git committed per CLAUDE.md. Follow-ups (separate tickets): preproc reconciliation + no-signing relay analysis (ps1:6734); convert Computer/SCCM_Site nodes+edges from smb_computers/smb_sites.

**2026-06-15T22:12:35Z**

Auth coverage fix: extended connect_smb (smb_sso.py) to accept nt_hash + kerberos_ticket + kdc_host, routing PtT->kerberosLogin(TGT), PtH->login(nthash), password->login, then SSPI, then null. Reuses http_auth.format_hashes + CCache/toTGT decode (same as wmi.py). collectors/smb.py and registry.py now pass ctx.nt_hash/ctx.kerberos_ticket + resolved DC as KDC -- so SMB AND RemoteRegistry now honor --nt-hash/--ticket (previously silently ignored). Signing check stays unauthenticated. Added 4 tests (3 routing in test_smb_sso.py, 1 forwarding in test_smb.py); updated CLI help (main.py) + README auth sections. Validation: pytest 299 passed/5 skipped, ruff clean on changed code (3 pre-existing F401 unused imports at top of main.py are NOT mine). Not committed.

**2026-06-15T22:19:27Z**

LIVE VALIDATION (lab mayyhem.com, debug_smb_auth.py) -- all 4 auth methods OK on every host: sspi(current-user), password(NTLM), pth(pass-the-hash), ptt(pass-the-ticket/kerberosLogin). check_smb_signing()->True everywhere (unauth negotiate). Classification on real shares: ps1-sms (provider-only: ADMIN$/C$/IPC$) -> is_sccm=False (correct). ps1-pss (site server) -> roles=[SMS Site Server@PS1, SMS Distribution Point@PS1], content_lib, site=PS1 from SMS_SITE. ps1-dp (DP) -> roles=[SMS Distribution Point@PS1], pxe=True (REMINST), content_lib, site=PS1 resolved from SMS_DP$ description via the bug-fixed share-description fallback (PS1's $smsSite.Description bug would have missed it on a DP-only host). Pass-the-hash + pass-the-ticket confirmed working against live SMB where they were previously ignored. Added debug_smb_auth.py (sibling of debug_wmi_auth.py); not committed.

**2026-06-16T14:25:22Z**

CRITICAL FIX: signing detection was reading impacket _Connection['RequireSigning'], which smb3.py:658-663 forces True for SMB 3.1.1 (default dialect on modern Windows) regardless of server policy -> every host falsely 'signing required', hiding relay targets. Replaced: check_smb_signing now does a raw SMB2 negotiate (SecurityMode bit 0x0002 @ offset 70, PS1-faithful); negotiated_signing_required reads _Connection['ServerSecurityMode'] (smb3.py:687, uncontaminated) for registry.py fallback, None for dialect<3.0. Live lab proof: dc/ps1-mp=True, ps1-sms/ps1-pss(site server)/ps1-dp(DP)=False (raw negotiate and ServerSecurityMode agree); old RequireSigning=True for all 5. +5 unit tests (parse/build/ServerSecurityMode regression). 304 passed/5 skipped, ruff clean. Not committed.
