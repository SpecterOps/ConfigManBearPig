---
id: ope-1f49
status: closed
deps: []
links: []
created: 2026-07-13T14:57:26Z
type: task
priority: 1
tags: [migration, shared-lib]
---

# Migrate SCCM onto openhound-collector-common shared library

Migrate the SCCM extension onto the openhound-collector-common shared library (editable path dep), deleting SCCM's duplicated infra. Plan: sccm/sccm/docs/superpowers/specs/2026-07-02-sccm-shared-lib-migration-design.md. Phases: 0 Foundation (merge+graph/gitignore fix+pyproject+unit green), 1 duckdb_safe, 2 log_context core+graph base, 3 source_bridge, 4 discovery/dns, 5 auth primitives, 6 clients/ad, 7 clients/wmi, 8 clients/mssql EPA, 9 cleanup+governance docs+full lab validation. Declines: convert_pipeline, socks (stay local). Read-only lib governance rule.

## Notes

**2026-07-13T14:57:36Z**

Phase 0 complete: (0a) integration branch = ohsccm + ohmssql merged clean; (0b) root-caused missing shared graph/ package to an unanchored '.gitignore graph' pattern, fixed with negation, package now tracked + 8/8 graph tests pass, committed 9d1f895; (0c) SCCM pyproject depends on the lib via editable path + [tool.uv.sources]; (0d) uv sync OK, impacket resolved to >=0.13.1, SCCM unit suite 582 passed/5 skipped - no regression. 0e (lab output baseline) deferred until before live-auth phases 5-8. Next: Phase 1 duckdb_safe.

**2026-07-13T16:46:55Z**

Phase 1 (duckdb_safe) complete: SCCM transforms.py now uses shared safe_execute/ensure_columns/arr_sql via thin local wrappers (names + call sites unchanged; SCCM logger + wmi/adminservice sibling-miss injected). Shared-lib change (user-approved Option 1): safe_execute/ensure_columns gained optional logger= (default module logger; mssql unaffected). Results: SCCM 582 passed/5 skipped (baseline parity), targeted 28 passed, mssql 172 passed, ruff clean, mypy no new errors (15 pre-existing at HEAD -> 14). Note: shared lib lacks py.typed (informational; deferred as hygiene item).

**2026-07-13T18:29:35Z**

Phase 2 (MINIMAL, per user decision) complete. (a) log_context.py sources VERBOSE from the shared lib (import registers addLevelName + Logger.verbose); local VERBOSE/_verbose removed; all SCCM-specific logging machinery (resource ctx, completion callbacks, _DebugExcInfoFilter, per_host/per_pair iterators, cached_with_log, trace_*, VerboseLogger, superset install_filter/with_log_context) kept LOCAL. (b) models/graph_edge.py now subclasses the shared graph base (same pattern as mssql): inherits fields/as_node/config, sets traversable_kinds=TRAVERSABLE_EDGE_KINDS, overrides edges for SCCM relay/lean CMBP-cased props. (c) pyproject: mypy override for openhound_collector_common.* (untyped; py.typed deferred as hygiene task). StubNode + full log_context core stay LOCAL (entangled/volatile glue, D5). No shared-lib change this phase -> mssql unaffected. Results: SCCM 582 passed/5 skipped (baseline parity), ruff clean, mypy no new errors (only pre-existing openhound.core untyped + markup/verbose).

**2026-07-13T18:40:39Z**

Phase 3 (source_bridge) DECLINED (principled divergence). Tried to unify the DONE marker + dedup the byte-identical stream primitives by re-exporting DONE/build_streams/broadcast_done from the shared source_bridge into phased_pipeline/streams.py. BLOCKED by a hard, test-enforced invariant: test_pp_engine::test_engine_source_imports_nothing_project_specific forbids 'dlt','ldap3','openhound','openhound_sccm' in ANY phased_pipeline import line -- the engine is deliberately a zero-dependency, liftable-into-its-own-package sub-package. Importing openhound_collector_common.dlt.source_bridge violates it (both 'openhound' and 'dlt' in the path); no alternative path exists (shared lib IS named openhound_collector_common). DONE is identity-compared, so a partial consumer-side adoption would deadlock (engine broadcasts its own DONE; shared StreamBridge.drain_stream hardcodes the shared DONE). Conclusion: SCCM's push->pull machinery is correctly coupled to its portable engine; the shared StreamBridge is the engine-LESS alternative for collectors like mssql. Keep SCCM's engine+streams+source.py emit machinery LOCAL. Reverted streams.py; engine tests green (18 passed). Emerging pattern: architecture-integrated subsystems (log_context, engine/source_bridge) resist sharing; remaining value is the leaf client stacks (dns, auth, ad, wmi, mssql-EPA).

**2026-07-13T18:56:41Z**

Phase 5 (auth leaf helpers) COMPLETE + LIVE-VALIDATED. http_auth.py re-exports EMPTY_LM_HASH/format_hashes/split_user_domain from shared clients/auth (byte-identical; local copies removed). Callers http.py/wmi.py/smb_sso.py import these from http_auth -> now transparently shared. smb_sso._split_user_domain kept LOCAL (different: NetBIOS short domain + None-guard, not the shared full-domain helper). mssql_epa's own copies deferred to Phase 8. Validation: unit 582 passed/5 skipped (parity), ruff clean, mypy no new errors. LIVE LAB (up): pre-migration baseline captured green first, then post-change SMB SSO (ps1-pss) + HTTP Negotiate (ps1-sms) all 3 identities = 4 integration tests passed. First phase validated end-to-end vs live lab per D8. NOTE Phase 3 declined earlier (portable-engine invariant). Lab connection for integration tests: OH_SSO_INTEGRATION=1; OPENHOUND_HTTP_TEST_TARGET, DOMAIN, KDC, USER, and the credential (PASSWORD or NTHASH) come from the environment -- see $SCCM_LAB_PASSWORD / $SCCM_LAB_NT_HASH, the same variables the dev/ scripts read. (Literal values were removed from this ticket on 2026-07-29 before the .tickets/ directory was copied into the public ConfigManBearPig repo.)

**2026-07-13T19:04:51Z**

Phase 4 (discovery/dns) DECLINED/deferred (marginal + intentional divergence). Shared resolve_dc queries _ldap._tcp.<domain> + A-fallback; SCCM's main.py _resolve_dc_via_dns intentionally queries _ldap._tcp.dc._msdcs.<domain> (precise DC record, matches .NET DsGetDcName / the PowerShell original) with no A-fallback -> adopting shared would change DC discovery on the auth-critical LDAP path. Other resolver sites (context.py resolve_ip = host->IP not DC discovery, configure=True; collectors/dns.py = ADIDNS MP-SRV collection phase, leave local) aren't resolve_dc. Only make_resolver (pure resolver builder) is adoptable and it's a ~4-line dedup with per-site config nuances -> not worth the churn/risk now. Keep SCCM DNS/DC discovery LOCAL. Optional make_resolver dedup can be a later cleanup ticket. PIVOT: lab is up -> prioritize high-value client swaps (Phase 6 ad, 7 wmi, 8 mssql-EPA) where dedup is large and live validation is essential.

**2026-07-13T19:23:47Z**

DIRECTION PIVOT (user, decisive): maximize mutual code between SCCM + mssql; effort/live-validation no object (dev env, sole user). Strategy flips from adopt-down (SCCM takes mssql-tuned shared) to PROMOTE-UP: reconcile both implementations into one shared SUPERSET (SCCM's tuning + mssql's extensions) that BOTH consume; nothing lost. Re-scoped order: 1 clients/ad (fold SCCM tuned lockout-safe waterfall + mssql modes into shared AdClient; SCCM keeps get_spns/attr-map + ADCredentials->LdapAuth adapter as thin subclass), 2 clients/auth (unify all token minting), 3 clients/wmi, 4 clients/mssql EPA, 5 discovery/dns (resolve_dc gains SRV-form option to preserve SCCM .dc._msdcs.), 6 log_context (shared absorbs SCCM superset; SCCM local copy removed), 7 source_bridge/engine (relax engine zero-dep TEST to allow shared stream primitives; unify DONE/StreamBridge - hardest/last). Validation: BOTH extensions' unit + live lab after each shared-lib change. Governance flips: shared lib = primary home for Windows-auth stack; both extensions thin consumers; update AGENTS.md + ARCHITECTURE.md. Supersedes earlier Phase 3/4/6 declines.

**2026-07-13T19:47:11Z**

RECONCILIATION #1 (clients/ad) COMPLETE + LIVE-VALIDATED (promote-up). SCCM ADClient is now a thin subclass of the shared AdClient: inherits the whole lockout-safe waterfall (bind/_build_attempt_plan/_select_auth_modes/_open_connection/_prepare_attempts_and_host/paged_search). SCCM ad.py 957 -> ~155 lines (~800 shared). SCCM keeps: ADCredentials->LdapAuth adapter (self.creds preserved for http.py/dns collector), snake_case _entry_to_dict override + _ATTR_KEY_MAP/_BINARY_ATTRS, get_spns; re-exports bytes_to_sid/guid. PROMOTED UP into shared (both extensions benefit): (a) paged_search gained controls= param (SD-flags to read ntSecurityDescriptor; mssql passes none = unchanged); (b) SCCM's IP-DC Kerberos->SSPI-NTLM reverse-DNS fallback moved into shared bind as _prepare_attempts_and_host (messages generalized, no SCCM CLI/env names). Tests test_ad_auth_warnings + test_principal_resolution_referral repointed to patch the shared module. VALIDATION: SCCM unit 582/5 skip; mssql unit 172; ruff clean; SCCM ad.py mypy CLEAN. LIVE LAB (explicit NTLM, -u domainadmin --dc dc.mayyhem.com): LDAP discovery via shared AdClient collected ldap_sites=2, management_points=1, cmrc_devices=14, system_management_dacl=5 (SD-controls path), zero errors. Remaining: #2 auth, #3 wmi, #4 mssql-EPA, #5 dns, #6 log_context, #7 source_bridge/engine.

**2026-07-13T20:51:48Z**

Reconciliation #2a (auth: choose_auth) COMPLETE + LIVE-VALIDATED (keystone). Promoted the credential-precedence ladder selector choose_auth (+ is_ip) into shared clients/auth (generalized messages). SCCM http_auth imports+re-exports choose_auth/is_ip from shared; removed local copies + unused ipaddress import. http.py + wmi.py get them via http_auth transparently -> shared WmiClient can now use the laddered choose_auth (#3). VALIDATION: SCCM unit 582/5skip; mssql 172; ruff clean; LIVE HTTP Negotiate (ps1-sms) all 3 identities passed. mssql_epa._choose_auth stays EPA-specific (explicit/sspi/skip contract, different from the rung-list). DEFERRED #2b (token-minting unification): refactor HTTP negotiators onto shared KerberosToken(gss_flags=_GSS_HTTP_FLAGS)/ntlm_type1,3(compute_mic=False,HTTP service)/SspiClient - isolated HTTP dedup, intricate NTLM-AV-pair tuning, NOT blocking; do after the client merges.

**2026-07-13T21:46:00Z**

Reconciliation #3 (clients/wmi) COMPLETE + LIVE-VALIDATED. SCCM wmi.py imports shared _ImpacketBackend/_PyWin32Backend (+ row normalization) from openhound_collector_common.clients.wmi and deletes its ~160-line copies (identical; shared generalized from SCCM: per-namespace DCOMConnection + embedded-Props unwrap). SCCM keeps only its WmiClient wrapper: choose_auth ladder (try rung, cache winner) + query(namespace,class,columns) + from_context. wmi.py 359->~200 lines. Shared WmiClient UNCHANGED -> mssql unaffected. test_wmi_client _normalize tests repointed to shared module (other tests use w._ImpacketBackend/_PyWin32Backend = the imported shared classes, still resolve). VALIDATION: SCCM unit 582/5skip; ruff clean; LIVE WMI vs ps1-sms SMS Provider (debug_wmi_auth.py) ALL 5 auth methods OK (sspi/password-kerberos/pth/ntlm-via-IP/ptt) - SMS_Site/SMS_SCI_SiteDefinition(embedded Props)/SMS_Admin 3 rows each.

**2026-07-13T21:59:32Z**

Reconciliation #4 (clients/mssql_epa) COMPLETE + LIVE-VALIDATED. SCCM mssql_epa.py is now a thin test_epa ADAPTER over shared openhound_collector_common.clients.mssql.detect_epa (TDS EPA probe matrix + TLS-1.2-capped login + channel-binding classification + Off/Allowed/Required decision tree generalized into shared, which mssql also uses). mssql_epa.py 675->~120 lines. SCCM keeps: test_epa (explicit/sspi/skip ladder -> shared Auth + pinned MSSQLSvc SPN -> detect_epa -> EPAResult), EPAResult, _select_mssql_spn, _sspi_available; re-exports EPAPrereqError (debug_epa_matrix). Uses shared split_user_domain. Allowed/Required SSPI label surfaced verbatim (feedback_epa_uncertainty_label). No shared mssql.py change -> mssql unaffected. TESTS: test_mssql_epa rewritten to test the adapter (SPN + ladder + verdict->EPAResult, detect_epa mocked); ~22 EPA-internals decision-tree unit tests DELETED (logic moved to shared, validated live). VALIDATION: SCCM unit 552/5skip green; ruff clean; LIVE test_epa vs ps1-db (explicit NTLM) coherent verdict (EP=Off, forceEnc=True). FOLLOW-UP: migrate EPA decision-tree unit tests into the shared lib suite (lacks them); full 12-config debug_epa_matrix available for exhaustive live run.

**2026-07-14T14:06:52Z**

Reconciliation #5 (discovery/dns) COMPLETE + live-validated. SCCM main.py _resolve_dc_via_dns now builds its resolver via shared discovery.dns.make_resolver (unifies the dns_resolver-set / host-default branches). SCCM KEEPS its own SRV query _ldap._tcp.dc._msdcs.<domain> (precise DC-Locator record, .NET Domain.FindDomainController; narrower than shared resolve_dc's _ldap._tcp.<domain>) + priority/weight sort + None-on-failure -> shared resolve_dc NOT adopted (intentional divergence). collectors/dns.py (ADIDNS MP-SRV, DC-as-nameserver) + context.resolve_ip (socket host->IP) stay LOCAL (different concerns). No shared change -> mssql unaffected. test_dns_resolver_flag 'not provided' test updated (make_resolver(None) builds a host Resolver() vs old module-level resolve). VALIDATION: SCCM unit 552/5skip green; ruff clean; LIVE _resolve_dc_via_dns('mayyhem.com') -> dc.mayyhem.com via make_resolver+SRV.

**2026-07-14T14:15:22Z**

Reconciliation #2b (auth token-minting) COMPLETE + LIVE-VALIDATED. SCCM http_auth KerberosNegotiator + SspiNegotiator are now thin wrappers over shared KerberosToken (gss_flags=_GSS_HTTP_FLAGS) + SspiClient -> deleted ~90 lines of duplicated AP-REQ minting (_service_ticket/_build_blob, byte-identical to KerberosToken) + the sspi.ClientAuth handshake; ruff auto-removed 22 now-dead impacket/pyasn1 imports. NtlmNegotiator KEPT local (HTTP-specific: no MIC, impacket-default service; not exercised live since kerberos wins the ladder for hostname targets -> refactoring unvalidated). No shared change (KerberosToken/SspiClient pre-existed) -> mssql unaffected. test_http_auth caching test repointed to neg._token; bad-ticket test still passes (ValueError from KerberosToken.__init__). VALIDATION: SCCM unit 552/5skip; http_auth+client units 32; ruff clean; LIVE HTTP Negotiate (ps1-sms) 3/3 (sspi via SspiClient, explicit+PtH via KerberosToken).

**2026-07-14T14:53:53Z**

Reconciliation #6 (log_context) COMPLETE (promote-up, generalized) + validated. Promoted SCCM's FULL log_context superset into shared openhound_collector_common.logging.log_context: contextvars(+resource)/target_context/phase_context/LogContextFilter/install_filter(superset)/with_log_context(supports BOTH target_from_ctx_domain[SCCM] + target_from_ctx_attr[mssql] + resource ctx + resource-complete callback firing)/completion-callback registry/per_host_iter/per_pair_iter/VerboseLogger/get_logger/_DebugExcInfoFilter. Collector-specific helpers GENERALIZED via logger param: cached_with_log(label, logger) + trace_node/edge/property/node_with_properties(..., logger). SCCM log_context.py -> re-export shared + thin trace_*/cached_with_log wrappers binding openhound_sccm.graph/openhound_sccm.lookup (call sites + log routing unchanged). shared logging/__init__ re-exports full public surface. mssql GAINS debug exc-info filter, dlt-logger [target][phase] tagging, VerboseLogger, iterators, resource ctx, generalized trace/cache. test_debug_exc_info_filter repointed to shared; test_per_host_log_blocks unchanged. VALIDATION: SCCM unit 552/5skip; mssql 172; shared log_context test 9; ruff clean; functional [MAYYHEM.COM][LDAP] prefix intact through re-exports (rewiring risk cleared). Overrides earlier D13 per user direction (mssql is also dev/single-user).

**2026-07-15T19:04:33Z**

Audit close (2026-07-15): shared-library migration complete through Reconciliation #7 + the shared-library governance flip. Note for record: ticket notes stop at #6; #7, the governance flip (ARCHITECTURE.md/AGENTS.md, commit 2cb30d9), the SCCM per-host streaming adoption of shared StreamBridge (3eca9c6), and the integration-test validation (f074fc1) exist as commits. Supersedes the bare duplicate ope-f651.
