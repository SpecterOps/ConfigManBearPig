---
id: con-7842
status: closed
deps: []
links: []
created: 2026-08-01T19:46:24Z
type: task
priority: 2
tags: [testing, integration, cli, compare]
---

# Adopt openhound-collector-common v0.1.2: baseline/candidate compare orientation, regression exit codes, three-way privilege flag

## Notes

**2026-08-01T19:46:42Z**

Adopts the v0.1.2 shared-library release. (1) compare_to_zip now passes the saved payload as BASELINE and this run's graph as CANDIDATE -- v0.1.2 flipped compare_graphs to (baseline, candidate), so the previous argument order silently inverted every report the moment the floor moved. (2) A regression (anything the baseline had and the candidate lost) now exits non-zero on both --compare-to-zip and openhound-compare; there is deliberately no opt-out, so a cross-tool CMBP diff exits non-zero by design. (3) --integration-privilege auto|high|low replaces --integration-lowpriv (see con-6677), with auto deriving the verdict from adminservice_*/wmi_* row counts. Remaining: dev/ab_matrix.py, README/ARCHITECTURE, CI list.

**2026-08-01T21:15:07Z**

Done. compare_to_zip now passes the saved payload as BASELINE and this run's graph as CANDIDATE, returning 1 on any regression; _combined_testing_exit_code reports the worse of both testing flags after both have run, retiring --compare-to-zip's always-exit-0 contract. --integration-privilege auto|high|low replaced --integration-lowpriv (see con-6677), with auto deriving the verdict from adminservice_*/wmi_* row counts via _detect_privileged -- prefix-matched, so the local_wmi_* discovery tables are not miscounted. Added dev/ab_matrix.py + dev/_ab_cmbp.py (the ported PowerShell drivers) and dev/regen_ticket_index.py with a --check mode wired into tests/regen_ticket_index_test.py. Four new offline test files added to both the CI list and the README command that mirrors it; README gained a Before and after a change section; ARCHITECTURE section 14 and a changelog entry updated. Suite 972 passed / 5 skipped (was 935); ruff and mypy clean. Not closed by this work: con-c542 (fixture tagging audit) and con-907c (rebaseline).
