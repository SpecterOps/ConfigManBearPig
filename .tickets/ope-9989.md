---
id: ope-9989
status: closed
deps: []
links: []
created: 2026-07-20T18:30:23Z
type: task
priority: 2
tags: [sccm, edge-help, docs]
---

# Make HasUser edges' abuse info less prescriptive

Rewrite the windowsAbuse prose on SCCM_HasPrimaryUser, SCCM_HasCurrentUser, and SCCM_HasADLastLogonUser to be tool-agnostic (drop mimikatz/meterpreter/PowerSploit/Cobalt Strike/Empire named walkthroughs) and prune/update the outdated references to durable MITRE ATT&CK technique pages. Leave general and opsec sections as-is. All three windowsAbuse blocks stay identical since the underlying session-credential-theft mechanism is the same. Content lives in src/openhound_sccm/edge_help.py.

## Notes

**2026-07-20T18:34:58Z**

Rewrote windowsAbuse (identical across all 3 blocks) to be tool-agnostic: describes credential dumping from LSASS, token theft/impersonation, and keylogging/clipboard capture as capabilities without naming mimikatz/meterpreter/PowerSploit/Cobalt Strike/Empire, plus a prerequisites paragraph (reachability + admin/SYSTEM + interactive logon) and the transience caveat. Replaced dated references (mimikatz blog+repo, adsecurity, mwrinfosecurity PDF, PowerSploit script, old attack.mitre.org/wiki URLs) with durable MITRE ATT&CK pages: TA0006, T1003, T1134, T1056.001. Kept each edge's MS doc as first ref and the SCCM log-files footer. general + opsec left as-is. Tests pass (13 passed, 1 skipped). Follow-up: MSSQL_HostFor block still names mimikatz (line ~1189) but is out of scope (separate extension/branch).

**2026-07-20T18:38:52Z**

Also updated MSSQL_HostFor windowsAbuse: genericized the outdated 'dump credentials from LSA secrets with mimikatz sekurlsa::logonpasswords' step to describe recovering the domain service account's cleartext credentials from the host (LSA secrets / LSA process memory) and deferring to References for current tooling. Kept the legitimate/current parts intact (sqlcmd SYSTEM access, single-user-mode sysadmin) to stay consistent with sibling MSSQL blocks that name current tools. Added MITRE T1003 (OS Credential Dumping) to references; kept the two MS docs. Verified: zero mimikatz/sekurlsa/meterpreter/PowerSploit/old-wiki references remain anywhere in edge_help.py. Tests: 13 passed, 1 skipped.

**2026-07-20T18:40:47Z**

Reverted the MSSQL_HostFor change per user request; that block is back to its original text (mimikatz sekurlsa::logonpasswords restored, T1003 ref removed). The three SCCM HasUser edge changes remain in place. Diff now touches only the HasUser blocks. Tests still green.
